# Remove TODO test

## Requirements

1. Navigate to main page
2. Verify the page loads successfully (HTTP 200)
3. Check that the page has a visible title or heading
4. Verify there are no JavaScript console errors on load

## Expected Behavior

- The heading text contains the expected first name (case-insensitive, ignore surrounding punctuation/whitespace).
- Page title contains “TODO APP”.
- Todo input/control is visible, indicating dashboard loaded.
- Add a new TODO called "check if todo removal works"
- Remove the TODO and check that it is removed